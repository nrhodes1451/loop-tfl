# Optional — register at https://api-portal.tfl.gov.uk/
TFL_APP_KEY=

# Required for `npm run extract-foi-layout` (vision extract of FOI depths / compass).
# Re-runs after override edits skip the API when data/pdf/.pages/*.layout.json exists.
ANTHROPIC_API_KEY=
